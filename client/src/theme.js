import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // You can use your desired primary color
    },
    secondary: {
      main: "#dc004e", // You can use your desired secondary color
    },
  },
});

export default theme;
